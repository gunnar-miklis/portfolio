# "Whitelist" approach 
# ignore all files (or at least all hidden files) by default and add exceptions to those files that were agreed to be added to the project.
# to ensure that private files are not committed accidentally 

coverage
node_modules
dist
*.log
*debug.*
*error.*

# ignoring all files that start with a dot.
.*

# add exceptions to the configuration files that belong to the project.
!.gitignore
!.eslintrc
!.eslintignore
!.prettierrc
!.prettierignore
!.editorconfig

# yarn 4.3.0
.yarn/*
!.yarn/
!.yarn/cache
!.yarn/sdks

# IDE-VSCode
.vscode/*
!.vscode/
!.vscode/settings.json
!.vscode/extensions.json
